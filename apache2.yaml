---
- name: Install Apache2
  hosts: 10.10.5.181
  become: no # This enables privilege escalation using sudo
  tasks:
    - name: Update APT package cache
      apt:
        update_cache: yes

    - name: Install Apache2
      apt:
        name: apache2
        state: present
